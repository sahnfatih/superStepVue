/*import { createPinia } from 'pinia';

const pinia = createPinia();

export const useAuthStore = pinia.store('auth', {
  state: () => ({
    user: null,
  }),

  actions: {
    setUser(user) {
      this.user = user;
    },

    clearUser() {
      this.user = null;
    },
  },
});

export { pinia };*/
